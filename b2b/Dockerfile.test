FROM python:3.13-slim

WORKDIR /app

COPY b2b/requirements.txt b2b/requirements-dev.txt .

RUN pip install --no-cache-dir -r requirements.txt -r requirements-dev.txt

COPY . .

CMD ["pytest", "tests/", "-v", "--cov=src", "--cov-report=term"]