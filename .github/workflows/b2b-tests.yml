name: B2B

on:
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        test-file:
          - US-B2B-01.py
          - US-B2B-02.py
          - US-B2B-03.py
          - US-B2B-04.py
          - US-B2B-05.py
          - US-B2B-07.py
          - US-B2B-08.py
          - US-B2B-09.py
          - US-B2B-11.py

    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: b2b
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r b2b/requirements.txt
          pip install -r b2b/requirements-test.txt

      - name: Prepare environment
        run: |
          cp .env.example b2b/.env
          sed -i 's/DB_HOST=.*/DB_HOST=localhost/' b2b/.env
          pip install ./shared

      - name: Run ${{ matrix.test-file }}
        working-directory: b2b
        run: pytest --asyncio-mode=auto tests/${{ matrix.test-file }} -v